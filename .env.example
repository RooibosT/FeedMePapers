# ============================================================
# Paper-AI 환경 변수 설정
# 이 파일을 .env로 복사한 후 값을 채워주세요
#   cp .env.example .env
# ============================================================

# ─── Notion API ───────────────────────────────────────────
# 1. https://www.notion.so/profile/integrations 에서 "New integration" 생성
# 2. Capabilities: Read content, Insert content, Update content 활성화
# 3. 아래에 Internal Integration Token 붙여넣기 (ntn_ 또는 secret_ 시작)
NOTION_TOKEN=

# 4. Notion에서 데이터베이스 생성 후, 해당 DB를 integration과 공유
#    DB 페이지 → ··· → Connect to → 생성한 integration 선택
# 5. DB URL에서 ID 복사: https://notion.so/workspace/DATABASE_ID?v=...
NOTION_DATABASE_ID=

# ─── Semantic Scholar API (선택사항) ──────────────────────
# https://www.semanticscholar.org/product/api#api-key-form
# 없어도 동작하지만, API key가 있으면 rate limit 10req/s (없으면 1req/s)
S2_API_KEY=
